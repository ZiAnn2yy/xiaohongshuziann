【项目铁律 - 零基础防屎山版】

1. 所有代码必须极简、可读：函数不超过50行，类不超过200行。
2. 每次修改只改一个文件或一个功能，改完必须加清晰中文注释。
3. 使用Pydantic模型统一输入输出，绝不允许返回字符串直接parse。
4. 所有Prompt必须放在单独的prompts.py文件里，不要写死在analyzer.py里。
5. 每次重构后，必须输出「本轮改动总结」给用户看（改了什么，为什么改）。
6. 禁止使用任何高级黑魔法（装饰器、元类、复杂异步等），保持Python初学者能看懂的风格。
7. 文件结构永远保持：
   backend/
   ├── __init__.py
   ├── models.py          # 所有Pydantic模型
   ├── prompts.py         # 所有Prompt模板
   ├── analyzer.py        # 核心分析逻辑
   ├── main.py            # FastAPI入口（如果有）
   └── utils.py           # 通用工具函数
8. GLM/Claude生成代码后，必须让我（用户）确认后再保存。
